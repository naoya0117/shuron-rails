services:
  web:
    image: ghcr.io/naoya0117/shuron-rails:latest
    command: bin/rails server -b 0.0.0.0
    ports:
      - "3000:3000"
    environment:
      - RAILS_ENV=development
      - RAILS_MASTER_KEY
    volumes:
      - .:/rails
      - storage:/rails/storage

volumes:
  storage:
